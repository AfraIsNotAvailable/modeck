# MoDeck — Claude Instructions

## Style
Never change app styling/visuals/layout/colors/fonts/spacing unless user explicitly requests it.

## Workflow
User writes code themselves. Show code blocks to copy. Wait for "check and move on."
When user says "check the code" — read and review. When "move on" — give next step.
